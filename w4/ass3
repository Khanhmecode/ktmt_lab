.text 
    li $s3,0            # $s3 = 0
    li $s1,-100         # $s1 = -100
    li $s2,10           # $s2 = 10
    slt $t1,$s2,$s1
    bne $t1,$zero,EXIT
    j label
label:
    addi $s3,$zero,10
EXIT: